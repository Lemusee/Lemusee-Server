server:
  port:

spring:
  profiles:
    include: secret

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:
    username:
    password:

  jpa:
    hibernate:
      ddl-auto:
    show-sql:
    properties:
      hibernate:
        format_sql:

  cache:
    type:
  redis:
    host:
    port:

jwt:
  secret:
    access:
    refresh:
  time:
    access:
    refresh:

logging:
  slack:
    webhook-uri:
  config: